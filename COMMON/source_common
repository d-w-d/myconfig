### Get dir this script is in:
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

# Source 'em:
source $DIR/common_aliases
source $DIR/common_env
source $DIR/common_functions
source $DIR/common_setup_scripts
source $DIR/common_shell_prompt

