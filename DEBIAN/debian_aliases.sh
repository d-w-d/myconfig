#!/usr/bin/env false

alias ls='ls -F --color' # Sort with color and file-type indicator
alias allsblk="lsblk -o name,fstype,size,mountpoint,partuuid,partlabel"
alias alportsinuse='sudo netstat -tulpn'
alias alwwwhtml='cd /var/www/html'

# Opens a file with default application on linux
alias open='xdg-open'

alias almachinemodel='sudo dmidecode -s system-product-name'
